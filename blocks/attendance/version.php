<?php
defined('MOODLE_INTERNAL') || die();

$plugin->version   = 2017062300;        // The current plugin version (Date: YYYYMMDDXX)
$plugin->requires  = 2012112900;        // Requires this Moodle version
$plugin->component = 'block_attendance'; // Full name of the plugin (used for diagnostics)
$plugin->cron = 300;
